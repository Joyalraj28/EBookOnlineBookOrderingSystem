@model AdminDashboard.Models.Table.Author

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>@Html.DisplayFor(model => model.name)</h4>
    <hr />
   
    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
<div id="btn-group">

    <input id="Delete-btn" type="submit" value="Delete" class="btn btn-default" />
    <label> | </label>
    <div id="Info-a">
        @Html.ActionLink("Cancel", "Index")
    </div>

</div>
    }
</div>
